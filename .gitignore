# Umgebungsvariablen
.env
.env.local
.env.*.local

# Datenbank-Backups
database/backups/

# Docker
.docker/

# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp
*.swo
*.backup

# Claude Code (lokale Einstellungen — ausser geteilte Skills und Commands)
.claude/*
!.claude/commands/
!.claude/skills/

# Externe Bibliotheks-Symlinks (lokal via DEV_LOCAL setzen)
.libs/*

# Referenz-PDFs (lokal, nicht im Repo)
*.pdf

# Strategie-Dokumente (privat, nicht öffentlich)
.strategy/

# Implementierungspläne (temporär, nicht öffentlich)
docs/superpowers/

# Superpowers-Tool-Scratch (sdd-Reports, Brainstorm — nicht im Repo)
.superpowers/

# Design-Handoff / Referenz-Material (lokal, große Binärdateien — nicht im Repo)
design_handoff_decisionmap_redesign-mit-SS/
design_claude_code_fail/

# Build-Logs (generiert von .templates/docker/build.sh)
.templates/docker/logs/
apps/frontend/
apps/backend/
apps/ai-service/
infrastructure/
